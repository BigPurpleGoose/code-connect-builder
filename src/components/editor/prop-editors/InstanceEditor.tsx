import React from "react";

export function InstanceEditor() {
  return (
    <p className="text-[11px] text-slate-400 italic">
      Maps a Figma <strong className="text-slate-600">instance swap</strong>{" "}
      property to this React prop. In Figma, this appears as a property that
      lets designers choose a different nested component (e.g. swap out an
      icon).
    </p>
  );
}
